# 本次会话收尾边界

用户于 2026-09-07 明确要求：后续开发先更仔细地明确边界，本次完成 baseline 评测后结束。

已完成收尾：B0 开发集 109/208（52.40%），详见 [baseline 报告](baseline-results.md)。后续排队阶段已拦截，本地实验进程已关闭，本次会话不再继续开发。

## 本次允许完成

- 完成已经启动的 `complete-dev-b0-01`：B0 使用 Haystack 原生内存向量检索，固定 208 题开发集、499 份文档、188,343 个片段，生成和 embedding 使用已授权的外部 API。
- 保留全部正确、错误作答、拒答和运行错误，生成汇总、分类、延迟、内存及 token 报告，说明适用范围。
- 保存当前代码与收尾记录。必要的进程收尾只涉及本地实验；本次不修改远端部署。

## 本次不再执行

- B1–B4 的完整评测、组件消融、调参及最终候选选择。
- 保留测试集 90 题的问答评测或最终协议冻结。
- Linux 导入、资源验收和部署；baseline 永不作为远端部署对象。
- 新功能、前端迁移或继续扩展服务能力。

本地 `.tools/baseline-only` 已阻止排队的后续阶段。恢复开发前应先确认新的范围，再显式解除限制，不能把先前大范围授权视为要求自动继续。

## 后续讨论需要明确

1. 首先验证 B1/B2，还是同时评测已经实现的 B3/B4；每个阶段以什么证据决定保留或移除。
2. 对正确率、错误作答率、拒答率、运行错误、延迟和 API 成本的具体取舍；现有目标仅要求最终正确率不低于 B0，没有硬延迟 SLA 或金额预算。
3. 最终上线范围：现有 API 契约是否足够、是否需要前端、访问方式与运维责任。
4. 实际 Linux 验收的工作负载、持续时间、原始 Office 预处理与归一化 Evidence 导入的边界。

远端此前只完成部分文件传输和工具下载，未启动新应用；旧 18443 站点及对应后端已停止。已上传的数据包早于补齐 32 份 Word 文档，未来部署必须核对并更新，不能直接作为最终数据使用。

旧索引上的 `dev-b0-01` 和中断的 `dev-b1-01` 仅作诊断记录。正式收尾的 baseline 是补齐语料后的 `complete-dev-b0-01`，不能混用两套语料的结果计算组件收益。
